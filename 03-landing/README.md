# JobDue Landing Workspace

This is the isolated JobDue landing page copy. It uses `https://app.hi-craniai.workers.dev` for staging metadata.

Before public launch, replace:

- `https://gumroad.com/l/jobdue-launch-placeholder` with the real JobDue checkout URL.
- `https://app.hi-craniai.workers.dev` with the final public domain if different.

Deployable files are copied into `release/jobdue/cloudflare-pages-jobdue-*.zip`.
