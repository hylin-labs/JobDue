# JobDue Listing Assets

Fresh JobDue listing images are in `assets/`.

The copied `design-html/` source has been text-rebranded for future manual export work, but the generated images in this folder are the current safe assets for release.
